[package]
name = "statistics"
version = "0.0.1"
edition = "2024"

[dependencies]
render_graph = { path = "../render_graph" }
resource_store = { path = "../resource_store" }
pipeline_store = { path = "../pipeline_store" }
gpu = { path = "../gpu" }
