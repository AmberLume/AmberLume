#[derive(Copy, Clone)]
pub struct SliceIndex {
    pub value: u32
}

#[derive(Copy, Clone)]
pub struct FrameIndex {
    pub value: u32
}

#[derive(Copy, Clone)]
pub struct ChunkIndex {
    pub value: u32
}
